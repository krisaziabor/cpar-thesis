/**
 * Spacer placed at the bottom of panel bodies so the floating nav does not
 * occlude the last row of content. The height matches the floating nav's
 * vertical footprint.
 */
export default function FloatingNavClearance() {
  return <div className="h-16" />;
}
